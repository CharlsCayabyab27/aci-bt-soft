// function handleMessage(event) {
//     for (var key in event.data) {
//         if (['var', 'num', 'day', 'sup', 'month', 'year'].includes(key)) {
//             document.getElementById(key).innerText = event.data[key] || '';
//         }
//     }
// }

// window.addEventListener('message', handleMessage);
